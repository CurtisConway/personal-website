{
  "scripts": {
    "prebuild": "rm -rf docs/*.png;rm -rf docs/*.js;rm -rf docs/*.css;rm -rf docs/*.html;rm -rf docs/*.map",
    "watch": "parcel index.html --log-level 4 --no-cache",
    "build": "parcel build index.html --log-level 4 --no-cache"
  },
  "devDependencies": {
    "autoprefixer": "^9.5.0",
    "cssnano": "^4.1.10",
    "postcss-modules": "^1.4.1"
  }
}
